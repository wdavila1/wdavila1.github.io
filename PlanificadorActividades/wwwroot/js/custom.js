/*Nombre de archivo: custom.js*/

$(function () {
    "use strict";

    /* Carrusel (ajusta el intervalo del carrusel)
    ------------------------------------------------------------------- */
    $('#myCarousel').carousel({
        interval: 5000 // Cambia el intervalo según sea necesario
    });
});
